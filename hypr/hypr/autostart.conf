# Hyprland Autostart Configuration

# Essential services
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Power management notifications (adjust path as needed)
# exec-once = upower-notify -tick 60s

# Autostart apps
exec-once = waybar
exec-once = wpaperd
exec-once = hypridle
